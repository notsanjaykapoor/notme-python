syntax = "proto3";

package stream;

service Stream {
  // A Bidirectional streaming RPC.
  //
  // Accepts a stream of Message sent while a route is being traversed,
  rpc rpc_stream(stream Message) returns (stream Message) {}
}

message Message {
  string message = 1;
}
